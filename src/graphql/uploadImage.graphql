mutation uploadImage($contextId: ID!,$upload: Upload!) {
   uploadImage(contextId: $contextId, upload:$upload ){
        id
        metadata {
            heightPx   
            widthPx
        }
        url 
    }
    
}