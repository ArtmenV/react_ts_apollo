import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';
import { PropsTable } from '@storybook/components';
import Button from './'
import {Settings} from 'react-feather'
import { action } from '@storybook/addon-actions';

<Meta title="Elements|Button" />

# Button
Let's define a story for the `button` elements

## Primary
<Preview>
  <Story name="Primary">
    <Button variant="primary">Primary</Button>
  </Story>
</Preview>

## Outline
<Preview>
  <Story name="Outline">
    <Button variant="outline">Outline</Button>
  </Story>
</Preview>


## Loading
<Preview>
  <Story name="loading">
    <div>
    <Button variant="primary" isSubmitting>Loader</Button>
    <hr />
    <Button variant="outline" isSubmitting>Loader</Button>
    </div>
  </Story>
</Preview>

## Icon
<Preview>
  <Story name="icon">
    <Button onClick={action('test')} isIcon variant="outline">
      <Settings size={18} color="#222" />
    </Button>
  </Story>
</Preview>